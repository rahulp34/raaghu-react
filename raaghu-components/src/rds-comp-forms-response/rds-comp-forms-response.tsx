import { RdsButton, RdsInput, RdsTextArea, RdsTextEditor } from "raaghu-react-elements";
import React, { useEffect, useState } from "react";

export interface RdsCompFormsResponseProps {
}

const RdsCompFormsResponse = (props: RdsCompFormsResponseProps) => {
    return (
        <>
        <h4 className="mt-7 text-center opacity-25">There is no response yet</h4>
        </>
    );
};
export default RdsCompFormsResponse;
