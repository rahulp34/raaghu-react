import React from "react";
import RdsCompLogin from "../../rds-comp-login/rds-comp-login";

export const code_actual = () => {
  return <RdsCompLogin onLogin={() => {}} onForgotPassword={() => {}} />;
};

export default code_actual;
