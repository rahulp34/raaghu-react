import React, { Suspense } from "react";
import IdentityResources from "./IdentityResources/IdentityResources";

const App = () => (
  <Suspense>
    <IdentityResources></IdentityResources>
  </Suspense>
);

export default App;
